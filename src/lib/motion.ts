/** Shared whileHover animation for every button in the app (motion/react). */
export const buttonHover = {
  whileHover: { scale: 1.1, transition: { duration: 0.1 } },
  transition: { duration: 0.5 },
} as const;
